Neutral bordered surface for dashboard content — insights panels, approval rows, settings groups.

```jsx
<Card><h3>Deze week</h3><p>82% van de taken afgerond.</p></Card>
```

`padded={false}` for cards that manage their own inner spacing (e.g. list rows).
