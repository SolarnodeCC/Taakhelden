Parent-dashboard sidebar navigation, copied from the real AppShell nav markup (Vandaag / Goedkeuren / Taken / Winkel / Inzichten).

```jsx
<SidebarNav items={[{key:'vandaag',label:'Vandaag'},{key:'winkel',label:'Winkel'}]} activeKey="vandaag" />
```
