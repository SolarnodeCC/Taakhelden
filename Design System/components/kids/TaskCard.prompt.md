Kid-app task row for the "Mijn Dag" home screen — icon, title, points, one big checkmark tap-target.

```jsx
<TaskCard icon="🧹" title="Kamer opruimen" points={10} done={false} onToggle={...} />
```

`done` swaps the tile to a soft turquoise fill with a filled checkmark button.
