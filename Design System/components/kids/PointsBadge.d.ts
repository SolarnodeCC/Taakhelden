/**
 * PointsBadge — total points pill for the kid-app header.
 * @startingPoint section="Kid App" subtitle="Points counter pill" viewport="400x90"
 */
export interface PointsBadgeProps { points: number; }
export function PointsBadge(props: PointsBadgeProps): JSX.Element;
