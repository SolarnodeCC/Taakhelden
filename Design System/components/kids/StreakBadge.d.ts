/**
 * StreakBadge — daily-streak pill ("streak-bescherming" forgives one missed
 * day/week per the product proposal's gamification rules).
 * @startingPoint section="Kid App" subtitle="Daily streak pill" viewport="400x90"
 */
export interface StreakBadgeProps { days: number; }
export function StreakBadge(props: StreakBadgeProps): JSX.Element;
