Avatar with level badge, for the app header and "Mijn Held" profile screen. Uses an emoji as a stand-in for the real (not-yet-supplied) avatar-library illustrations.

```jsx
<AvatarBadge emoji="🦊" level={4} size={72} />
```
