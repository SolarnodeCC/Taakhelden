# Structured migration evidence for Jankurai HLT-030 (sql-bad-behavior).
# Runtime engine is Cloudflare D1 / SQLite.

engine = "sqlite"
phase = "expand"
risk = "low"
owner = "api"
approval = "ADR-0006-ai-policy-and-approved-use-cases"
affected_objects = [
  "task_proposals.review_flag",
]
lock_timeout = "n/a-sqlite-d1"
statement_timeout = "n/a-sqlite-d1"
rollback = "roll-forward only; additive nullable column"
backup = "D1 point-in-time / local wrangler state"
verify = "apps/api/test/proposals.test.ts"
