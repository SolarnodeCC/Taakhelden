// GENERATED by packages/shared/scripts/generate-swift-contract.ts — do not edit.
import Foundation

struct APIErrorEnvelope: Codable, Error {
    struct Body: Codable {
        let code: String
        let message: String
    }

    let error: Body
}

struct ChildProfileDTO: Codable, Equatable {
    let id: String
    let displayName: String
    let avatarId: String?
    let ageMode: String
}

struct FamilyCodeResultDTO: Codable {
    let familyName: String
    let children: [ChildProfileDTO]
}

struct ChildSessionResultDTO: Codable {
    let accessToken: String
    let refreshToken: String
    let expiresIn: Int
    let child: ChildProfileDTO
}

struct ParentSessionResultDTO: Codable {
    let userId: String
    let familyId: String
    let accessToken: String
    let refreshToken: String
    let expiresIn: Int
}

struct MemberViewDTO: Codable {
    let id: String
    let role: String
    let displayName: String
    let avatarId: String?
    let ageMode: String
    let birthYear: Int?
}

struct ParentFamilyViewDTO: Codable {
    let viewer: String
    let id: String
    let name: String
    let timezone: String
    let inviteCode: String
}

struct InstanceViewDTO: Codable, Identifiable, Equatable {
    let id: String
    let taskId: String
    let childId: String
    let date: String
    let status: String
    let title: String
    let icon: String?
    let category: String
    let points: Int
    let photoBonusPoints: Int
    let approvalRequired: Bool
    let daypart: String?
    let photoId: String?
    let photoStatus: String?
    let pointsEarned: Int?
    let redoNote: String?
    let completedAt: String?
    let approvedAt: String?
    let updatedAt: String?
}

struct TodayBalanceDTO: Codable, Equatable {
    let childId: String
    let balance: Int
    let todayCompleted: Int
    let todayTotal: Int
    let weekProgress: Double
    let streakDays: Int
    let lifetimeEarned: Int
}

struct ChildTodayViewDTO: Codable, Equatable {
    let viewer: String
    let date: String
    let instances: [InstanceViewDTO]
    let balance: TodayBalanceDTO
}

struct CompleteResultDTO: Codable {
    let pointsEarned: Int
    let photoBonusPoints: Int
    let dayBonusEarned: Bool
    let weekBonusEarned: Bool
    let newBadges: [BadgeDTO]
    let newBalance: Int
}

struct BadgeDTO: Codable, Equatable {
    let id: String
    let title: String
    let icon: String
}

struct ChildRewardViewDTO: Codable, Identifiable, Equatable {
    let id: String
    let title: String
    let icon: String?
    let price: Int
    let limitPerWeek: Int?
    let affordable: Bool
    let pinned: Bool
}

struct SavingsGoalViewDTO: Codable, Equatable {
    let rewardId: String
    let title: String
    let icon: String?
    let price: Int
    let progress: Double
}

struct ChildRewardsViewDTO: Codable {
    let viewer: String
    let balance: Int
    let rewards: [ChildRewardViewDTO]
    let savingsGoal: SavingsGoalViewDTO?
}

struct RedemptionViewDTO: Codable, Identifiable, Equatable {
    let id: String
    let rewardId: String
    let title: String
    let icon: String?
    let price: Int
    let childId: String
    let status: String
    let createdAt: String
    let handledAt: String?
}

struct ChildRedemptionsViewDTO: Codable {
    let viewer: String
    let redemptions: [RedemptionViewDTO]
}

struct RedeemResultDTO: Codable {
    let redemptionId: String
    let rewardId: String
    let price: Int
    let status: String
    let newBalance: Int
}

struct SyncMutationDTO: Codable {
    let key: String
    let op: String
    let instanceId: String?
    let rewardId: String?
    let photoId: String?
    let at: String?
}

struct SyncBodyDTO: Codable {
    let since: String?
    let mutations: [SyncMutationDTO]
}

struct SyncResultDTO: Codable {
    let key: String
    let status: String
    let points: Int?
    let newBalance: Int?
    let code: String?
    let message: String?
}

struct SyncChangesDTO: Codable {
    let ledger: [[String: JSONValue]]
    let instances: [InstanceViewDTO]
    let rewards: [ChildRewardViewDTO]?
}

struct SyncResponseDTO: Codable {
    let results: [SyncResultDTO]
    let changes: SyncChangesDTO
    let serverTime: String
}

struct UploadIntentResponseDTO: Codable {
    let photoId: String
    let uploadUrl: String
}

struct PhotoStatusResponseDTO: Codable {
    let photoId: String
    let status: String
    let url: String?
}

struct AvatarCatalogItemDTO: Codable, Identifiable, Equatable {
    let id: String
    let slot: String
    let unlockType: String
    let unlockThreshold: Int
    let unlockBadgeId: String?
    let previewEmoji: String
    let title: String
    let sortOrder: Int
}

struct AvatarCatalogResponseDTO: Codable {
    let items: [AvatarCatalogItemDTO]
}

struct MemberAvatarEquippedDTO: Codable, Equatable {
    let hat: String?
    let background: String?
    let accessory: String?
}

struct MemberAvatarStateDTO: Codable, Equatable {
    let memberId: String
    let equipped: MemberAvatarEquippedDTO
    let unlocked: [String]
    let level: Int
    let lifetimeEarned: Int
}

struct FamilyGoalDTO: Codable, Identifiable, Equatable {
    let id: String
    let title: String
    let icon: String
    let targetPoints: Int
    let childIds: [String]
    let startedAt: String
    let completedAt: String?
    let status: String
}

struct FamilyGoalProgressDTO: Codable, Equatable {
    let goalId: String
    let title: String
    let icon: String
    let earnedPoints: Int
    let targetPoints: Int
    let status: String
}

struct FamilyGoalProgressResponseDTO: Codable {
    let progress: FamilyGoalProgressDTO?
}

enum JSONValue: Codable, Equatable {
    case string(String)
    case int(Int)
    case double(Double)
    case bool(Bool)
    case object([String: JSONValue])
    case array([JSONValue])
    case null

    init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if container.decodeNil() {
            self = .null
        } else if let value = try? container.decode(Bool.self) {
            self = .bool(value)
        } else if let value = try? container.decode(Int.self) {
            self = .int(value)
        } else if let value = try? container.decode(Double.self) {
            self = .double(value)
        } else if let value = try? container.decode(String.self) {
            self = .string(value)
        } else if let value = try? container.decode([String: JSONValue].self) {
            self = .object(value)
        } else if let value = try? container.decode([JSONValue].self) {
            self = .array(value)
        } else {
            throw DecodingError.dataCorruptedError(in: container, debugDescription: "Unsupported JSON value")
        }
    }

    func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .string(let value): try container.encode(value)
        case .int(let value): try container.encode(value)
        case .double(let value): try container.encode(value)
        case .bool(let value): try container.encode(value)
        case .object(let value): try container.encode(value)
        case .array(let value): try container.encode(value)
        case .null: try container.encodeNil()
        }
    }
}
